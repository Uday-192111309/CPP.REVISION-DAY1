#include<iostream>
using namespace std;
int main()
{
	int i,n, div,sum=0;
	cout<<" enter the number :";
	cin>>n;
	for (i =1;i<n;i++)
	{
		div =n % i;
		if(div==0)
		 sum = sum + i;
	}
	if(sum==n)
	  cout<<" the number is  perfect number";
	else
	  cout<<" the number is  not perfect number";
}
